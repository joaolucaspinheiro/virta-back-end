# Copie para application-secrets.properties e preencha com suas credenciais.
# O arquivo application-secrets.properties esta no .gitignore e nunca deve ser commitado.

spring.datasource.url=jdbc:mysql://localhost:3306/virta_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=America/Sao_Paulo
spring.datasource.username=SEU_USUARIO
spring.datasource.password=SUA_SENHA
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Chave Base64 para assinar os JWTs (minimo 32 bytes / 256 bits)
# Para gerar: openssl rand -base64 32
api.security.token.secret=SUA_CHAVE_BASE64_AQUI
